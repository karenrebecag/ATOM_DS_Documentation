# ATOM Design System — Button Component Implementation Prompt

---

## ⚠️ CRITICAL INSTRUCTIONS FOR AI CODE GENERATORS (v0, Cursor, Claude, etc.)

**THIS IS THE ATOM BUTTON COMPONENT — NOT A shadcn/ui BUTTON**

### 🚫 FORBIDDEN — DO NOT USE:

**Prohibited Packages:**
- ❌ `@radix-ui/react-*` — Do NOT use Radix Primitives
- ❌ `tailwindcss` — Do NOT use Tailwind classes
- ❌ `class-variance-authority` — Do NOT use CVA
- ❌ `framer-motion` — Must use GSAP
- ❌ `@types/gsap` — GSAP includes types natively

**Prohibited Code Patterns:**
- ❌ Tailwind classes: `className="bg-blue-500 px-4 py-2"`
- ❌ Inline styles with hardcoded values: `style={{ padding: '8px' }}`
- ❌ CSS-only hover effects: `.button:hover { transform: translateY(-2px); }`
- ❌ Framer Motion: `<motion.button whileHover={{ scale: 1.05 }} />`
- ❌ Emojis as icons: `<button>⚡ Click me</button>`

### ✅ REQUIRED — YOU MUST USE:

**Required Packages:**
```bash
@atomchat.io/tokens@1.0.3
@atomchat.io/css@0.2.3
@atomchat.io/components-astro@2.0.4  # If using Astro
@atomchat.io/animations@1.2.1        # If animations enabled
gsap@3.14.2                          # If animations enabled
lucide-react@latest                  # For icons
```

**Required Styling Pattern:**
```tsx
// ✅ CORRECT: Use CSS custom properties
<button style={{
  background: 'var(--bg-primary)',
  padding: 'var(--m) var(--l)',
  borderRadius: 'var(--radius-m)'
}} />

// ❌ WRONG: Tailwind classes
<button className="bg-blue-500 px-4 py-2 rounded-lg" />
```

**Required Animation Pattern (if enabled):**
```tsx
// ✅ CORRECT: Use data attributes + initAllAnimations()
<button data-button data-hover-rotate>
  <span data-button-label>Click me</span>
  <span data-button-label-clone>Click me</span>
</button>

// Initialize once in layout/root:
import { initAllAnimations } from '@atomchat.io/animations';
useEffect(() => { initAllAnimations(); }, []);

// ❌ WRONG: CSS-only animations
<button className="hover:scale-105 transition" />

// ❌ WRONG: Framer Motion
<motion.button whileHover={{ scale: 1.05 }} />
```

**Required Icon Pattern:**
```tsx
// ✅ CORRECT: Lucide React icons
import { ArrowRight } from 'lucide-react';
<button>
  Get Started
  <ArrowRight size={20} />
</button>

// ❌ WRONG: Emojis
<button>Get Started ➡️</button>

// ❌ WRONG: Heroicons or other libraries
import { ArrowRightIcon } from '@heroicons/react/24/solid';
```

---

I need to implement the Button component from the ATOM Design System. Before we start, I need to answer these questions:

## Question 1: Target Stack
Which technology stack are you building this component for?

**Options:**
- **Astro** → Use the official npm package directly
- React (with TypeScript) → Convert from Astro source
- Next.js (App Router with TypeScript) → Convert from Astro source
- Vue 3 (with Composition API) → Convert from Astro source
- Angular (Standalone Components) → Convert from Astro source
- Svelte/SvelteKit → Convert from Astro source
- HTML/CSS/JavaScript (Vanilla) → Convert from Astro source

## Question 2: Animation Support
Do you want to include GSAP hover rotation animations?

**Options:**
- Yes, include full GSAP animations (hover rotation, shimmer effects)
- No, static component only (CSS-only states, no animations)

---

# Installation Instructions

## If you chose Astro:

```bash
# Install all required packages with exact versions
pnpm add @atomchat.io/components-astro@2.0.4
pnpm add @atomchat.io/tokens@1.0.3
pnpm add @atomchat.io/css@0.2.3

# If animations enabled, also install:
pnpm add @atomchat.io/animations gsap
```

**⚠️ CRITICAL: DO NOT install @types/gsap**
- GSAP 3.5+ includes TypeScript types natively in the main package
- The `@types/gsap` package is obsolete (last version: 3.0.0 from 2019)
- Only install `gsap` — no separate @types package needed
- Attempting to install @types/gsap will cause installation errors

### Install Lucide Icons (REQUIRED for icons)
```bash
pnpm add lucide-react
```

**🚫 CRITICAL RULE: NEVER USE EMOJIS**
- **NEVER use emojis in buttons or any UI** unless the user explicitly requests them
- **ALWAYS use Lucide React icons** for all visual icons in buttons
- Emojis are inconsistent across platforms and break professional design
- Browse icons at: https://lucide.dev/icons/

**Usage (Astro):**
```astro
---
import { Button } from '@atomchat.io/components-astro';
import '@atomchat.io/css';
---

<Button variant="Primary" size="m">
  Click me
</Button>
```

**Component source location in npm:**
`@atomchat.io/components-astro/src/atoms/Button.astro`

**✅ You're done!** The component is ready to use. No need to replicate anything.

---

## If you chose another framework (React/Vue/Next.js/etc.):

**Note:** The official npm package only contains Astro components. You'll need to convert the component to your chosen framework while maintaining the same design tokens, structure, and behavior.

```bash
# Install design tokens and styles (required)
pnpm add @atomchat.io/tokens@1.0.3
pnpm add @atomchat.io/css@0.2.3

# If animations enabled, also install:
pnpm add @atomchat.io/animations gsap
```

**⚠️ CRITICAL: DO NOT install @types/gsap**
- GSAP 3.5+ includes TypeScript types natively
- The `@types/gsap` package is obsolete (last release: 3.0.0 from 2019)
- Only install `gsap` — no separate @types package needed
- Installing @types/gsap will cause errors

**Reference implementation:**
- Source: `@atomchat.io/components-astro/src/atoms/Button.astro`
- npm registry: https://www.npmjs.com/package/@atomchat.io/components-astro

---

# Component Specification

## Overview
The Button component is the primary interactive element with:
- 6 visual variants (Primary, Secondary, Tertiary, Destructive variants)
- 5 sizes (xs, s, m, l, xl)
- Loading state with spinner
- Icon support (left, right, or both)
- Disabled state
- Can render as `<button>` or `<a>` tag
- Token-driven styling (NEVER hardcode values)

## NPM Package Information

**Package:** `@atomchat.io/components-astro@2.0.4`
**Component path:** `src/atoms/Button.astro`
**Dependencies:**
- `@atomchat.io/tokens@1.0.3` (design tokens)
- `@atomchat.io/css@0.2.3` (compiled CSS from tokens)

**Optional dependencies (for animations):**
- `@atomchat.io/animations` (GSAP animation functions)
- `gsap` (animation library)

## Props API

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| variant | "Primary" \| "Secondary" \| "Tertiary" \| "Destructive Primary" \| "Destructive Secondary" \| "Destructive Tertiary" | "Primary" | Visual style variant |
| size | "xs" \| "s" \| "m" \| "l" \| "xl" | "m" | Size of the button |
| href | string? | — | If provided, renders as `<a>` tag |
| type | "button" \| "submit" \| "reset" | "button" | Button type (only for `<button>`) |
| disabled | boolean? | false | Disables interaction |
| loading | boolean? | false | Shows loading spinner |
| class | string? | — | Additional CSS classes |

## Slots/Children

| Slot | Description |
|------|-------------|
| default | Button label text (required) |
| icon-left | Icon placed before label |
| icon-right | Icon placed after label |

## DOM Structure (Default State)

```html
<button class="button button--primary button--m" data-button data-hover-rotate>
  <span class="button__label-wrap">
    <!-- Original label -->
    <span class="button__label" data-button-label>
      <span class="button__icon" data-icon-left><!-- icon-left --></span>
      Button Text
      <span class="button__icon" data-icon-right><!-- icon-right --></span>
    </span>

    <!-- Clone label (for animation, hidden by default) -->
    <span class="button__label button__label--clone" data-button-label-clone aria-hidden="true">
      <span class="button__icon"><!-- icon-left clone --></span>
      Button Text
      <span class="button__icon"><!-- icon-right clone --></span>
    </span>
  </span>
</button>
```

## DOM Structure (Loading State)

```html
<button class="button button--primary button--m button--loading" disabled aria-busy="true">
  <span class="button__loading-content">
    <span class="button__loading-text" data-shimmer>Loading</span>
    <span class="button__spinner" data-button-spinner></span>
  </span>
</button>
```

## Required Design Tokens

### Size Tokens
```css
/* XS */
padding: var(--xxs) var(--xs);  /* ~4px 8px */
font-size: var(--text-label-regular);

/* S */
padding: var(--xs) var(--s);    /* ~8px 12px */
font-size: var(--text-label-regular);

/* M */
padding: var(--sm) var(--m);    /* ~12px 16px */
font-size: var(--text-label-regular);

/* L */
padding: var(--m) var(--l);     /* ~16px 24px */
font-size: var(--text-body-regular);

/* XL */
padding: var(--l) var(--xl);    /* ~24px 32px */
font-size: var(--text-body-large);
```

### Variant Tokens

**Primary:**
```css
background: var(--buttons-bg-primary-enabled);
color: var(--buttons-fg-primary-enabled);

/* States */
&:hover { background: var(--buttons-bg-primary-hovered); }
&:focus {
  background: var(--buttons-bg-primary-focused);
  box-shadow: var(--buttons-shadow-focus);
  border: 1px solid var(--buttons-border-primary-focused);
}
&:active { background: var(--buttons-bg-primary-pressed); }
&[disabled] {
  background: var(--bg-disabled);
  color: var(--fg-disabled);
  cursor: not-allowed;
}
```

**Secondary:**
```css
background: var(--buttons-bg-secondary-enabled);
color: var(--buttons-fg-secondary-enabled);
border: var(--stroke-xs) solid var(--buttons-border-secondary-enabled);

/* States */
&:hover { background: var(--buttons-bg-secondary-hovered); }
&:focus {
  background: var(--buttons-bg-secondary-focused);
  box-shadow: var(--buttons-shadow-focus);
}
&:active { background: var(--buttons-bg-secondary-pressed); }
```

**Tertiary:**
```css
background: var(--buttons-bg-tertiary-enabled);
color: var(--buttons-fg-tertiary-enabled);

/* States */
&:hover { background: var(--buttons-bg-tertiary-hovered); }
&:focus {
  background: var(--buttons-bg-tertiary-focused);
  box-shadow: var(--buttons-shadow-focus);
}
&:active { background: var(--buttons-bg-tertiary-pressed); }
```

**Destructive Primary:**
```css
background: var(--buttons-bg-danger-primary-enabled);
color: var(--buttons-fg-danger-primary-enabled);

/* States */
&:hover { background: var(--buttons-bg-danger-primary-hovered); }
&:focus {
  background: var(--buttons-bg-danger-primary-focused);
  box-shadow: var(--buttons-shadow-focus-danger);
}
&:active { background: var(--buttons-bg-danger-primary-pressed); }
```

**Destructive Secondary:**
```css
background: var(--buttons-bg-danger-secondary-enabled);
color: var(--buttons-fg-danger-secondary-enabled);
border: var(--stroke-xs) solid var(--buttons-border-danger-secondary-enabled);

/* States */
&:hover { background: var(--buttons-bg-danger-secondary-hovered); }
&:focus {
  background: var(--buttons-bg-danger-secondary-focused);
  box-shadow: var(--buttons-shadow-focus-danger);
}
&:active { background: var(--buttons-bg-danger-secondary-pressed); }
```

**Destructive Tertiary:**
```css
background: var(--buttons-bg-danger-tertiary-enabled);
color: var(--buttons-fg-danger-tertiary-enabled);

/* States */
&:hover { background: var(--buttons-bg-danger-tertiary-hovered); }
&:focus {
  background: var(--buttons-bg-danger-tertiary-focused);
  box-shadow: var(--buttons-shadow-focus-danger);
}
&:active { background: var(--buttons-bg-danger-tertiary-pressed); }
```

### Structure Tokens
```css
border-radius: var(--s);                /* 8px */
border-width: var(--stroke-xs);         /* 1px */
font-weight: var(--font-weight-medium); /* 500 */
gap: var(--xxs);                        /* Icon-to-text gap */
--icon-s: 16px;                         /* Icon size */
```

## Animation System (if enabled)

### Overview

The button uses a **3-function animation system** that must be initialized in this order:

1. **`initRotateClones()`** — Detects/creates clone labels (pre-render pattern)
2. **`initRotateCalc()`** — Calculates `--y` transform-origin based on text length
3. **`initHoverRotate()`** — Executes rotation animation on hover

### Data Attributes Contract
- `data-button` — Root element (discovery)
- `data-hover-rotate` — Enables hover rotation
- `data-button-label` — Original label (animated)
- `data-button-label-clone` — Clone label (must be pre-rendered with `aria-hidden="true"`)
- `data-clones-ready` — Set by `initRotateClones()` when setup complete
- `data-shimmer` — Loading text shimmer effect
- `data-button-spinner` — Loading spinner element

### Required CSS Variables
```css
.button {
  --r: 20deg;      /* Rotation angle (read by GSAP) */
  --y: 1100%;      /* Transform-origin Y (auto-calculated by initRotateCalc) */
}
```

### Critical CSS Structure

**Label Wrap (Grid for stacking original + clone):**
```css
.button__label-wrap {
  display: grid;
  place-items: center;
  overflow: hidden;
}
```

**Labels (both original and clone):**
```css
.button__label {
  grid-area: 1 / 1;  /* Stack in same grid cell */
  display: inline-flex;
  align-items: center;
  gap: var(--s);
  white-space: nowrap;
  transform-origin: 50% var(--y, 1100%);
  transform: rotate(0deg);
}

.button__label--clone {
  /* Initial state: rotated backwards and invisible */
  transform: rotate(calc(var(--r) * -1));
  opacity: 0;
}
```

### GSAP Configuration (config.ts)

**Register custom ease and set defaults:**
```typescript
import gsap from 'gsap';
import { CustomEase } from 'gsap/CustomEase';

gsap.registerPlugin(CustomEase);

// Create "atom" ease curve (matches design tokens)
CustomEase.create('atom', '0.625, 0.05, 0, 1');

// Duration constants (matches design tokens)
export const DURATION = {
  quarter: 0.15,
  half: 0.3,
  threeQuarters: 0.45,
  default: 0.6,
} as const;

// Set global defaults
gsap.defaults({
  ease: 'atom',
  duration: DURATION.default,
});

export { gsap };
```

### Function 1: initRotateClones()

**Purpose:** Detect pre-rendered clones or create them dynamically

```typescript
import { gsap } from './config';

const LABEL_SELECTORS = '.button__label';

export function initRotateClones() {
  const elements = document.querySelectorAll<HTMLElement>('[data-hover-rotate]');
  if (!elements.length) return () => {};

  elements.forEach((el) => {
    if (el.hasAttribute('data-clones-ready')) return;

    // Check if clone is pre-rendered (Astro pattern)
    const hasPreRenderedClone = el.querySelector('[aria-hidden="true"]');
    if (hasPreRenderedClone) {
      el.setAttribute('data-clones-ready', '');
      return;
    }

    // Fallback: create clone dynamically (non-Astro)
    const items = el.querySelectorAll<HTMLElement>(LABEL_SELECTORS);
    if (!items.length) return;

    const r = parseFloat(getComputedStyle(el).getPropertyValue('--r')) || 20;

    items.forEach((item) => {
      const clone = item.cloneNode(true) as HTMLElement;
      clone.setAttribute('aria-hidden', 'true');
      clone.style.position = 'absolute';
      clone.style.width = '100%';
      clone.style.height = '100%';
      clone.style.transformOrigin = `50% var(--y, 1100%)`;
      clone.style.transform = `rotate(${-r}deg)`;
      clone.style.opacity = '0';

      item.parentElement!.style.position = 'relative';
      item.parentElement!.appendChild(clone);
    });

    el.setAttribute('data-clones-ready', '');
  });

  return () => {}; // No cleanup needed (DOM structure)
}
```

### Function 2: initRotateCalc()

**Purpose:** Calculate `--y` transform-origin based on text length

```typescript
import { gsap } from './config';

const LABEL_SELECTORS = '.button__label';
const Y_PADDING = 30;
const Y_BASE_OFFSET = 12;
const Y_CHAR_MULTIPLIER = 6;
const Y_MIN = 100;
const Y_MAX = 10000;

export function initRotateCalc() {
  const elements = document.querySelectorAll<HTMLElement>('[data-hover-rotate]');
  if (!elements.length) return () => {};

  function getMaxChars(el: HTMLElement): number {
    const labels = el.querySelectorAll<HTMLElement>(LABEL_SELECTORS);
    if (!labels.length) return (el.textContent || '').trim().length;

    let max = 0;
    labels.forEach((label) => {
      // Skip clones (only count originals)
      if (label.getAttribute('aria-hidden') === 'true') return;
      const len = (label.textContent || '').trim().length;
      if (len > max) max = len;
    });
    return max;
  }

  function computeY(chars: number): number {
    let y = Math.round(Y_MIN + Y_PADDING * (Y_BASE_OFFSET + Y_CHAR_MULTIPLIER * chars));
    return Math.max(Y_MIN, Math.min(y, Y_MAX));
  }

  function update(el: HTMLElement) {
    const chars = getMaxChars(el);
    const y = computeY(chars);
    el.style.setProperty('--y', y + '%');
  }

  // Initial calculation
  elements.forEach((el) => update(el));

  // Watch for text changes
  const observer = new MutationObserver((mutations) => {
    const touched = new Set<HTMLElement>();
    mutations.forEach((m) => {
      const target = m.target.nodeType === Node.TEXT_NODE
        ? m.target.parentElement
        : (m.target as HTMLElement);
      if (!target) return;
      const root = target.closest<HTMLElement>('[data-hover-rotate]');
      if (root) touched.add(root);
    });
    touched.forEach((el) => update(el));
  });

  observer.observe(document.body, {
    characterData: true,
    childList: true,
    subtree: true,
  });

  // Recalculate on resize
  let resizeTimer: ReturnType<typeof setTimeout>;
  const onResize = () => {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(() => elements.forEach((el) => update(el)), 150);
  };
  window.addEventListener('resize', onResize);

  return () => {
    observer.disconnect();
    window.removeEventListener('resize', onResize);
    clearTimeout(resizeTimer);
  };
}
```

### Function 3: initHoverRotate()

**Purpose:** Execute rotation animation on hover

```typescript
import { gsap, DURATION } from './config';

const LABEL_SELECTORS = '.button__label';

function prefersReducedMotion(): boolean {
  return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}

function isClone(item: HTMLElement): boolean {
  return (
    item.getAttribute('aria-hidden') === 'true' ||
    item.classList.contains('button__label--clone')
  );
}

export function initHoverRotate() {
  const elements = document.querySelectorAll<HTMLElement>('[data-hover-rotate]');
  if (!elements.length) return () => {};

  const listeners: Array<{ el: HTMLElement; handler: EventListener }> = [];

  elements.forEach((el) => {
    let lastTime = 0;
    const COOLDOWN = 100; // Prevent spam

    const onEnter = () => {
      if (prefersReducedMotion()) return;

      const now = performance.now();
      if (now - lastTime < COOLDOWN) return;
      lastTime = now;

      const targets = el.querySelectorAll<HTMLElement>(LABEL_SELECTORS);
      const items = targets.length ? Array.from(targets) : [el];

      const r = parseFloat(getComputedStyle(el).getPropertyValue('--r')) || 20;
      const duration = DURATION.half; // 0.3s

      const originals: HTMLElement[] = [];
      const clones: HTMLElement[] = [];
      items.forEach((item) => {
        if (isClone(item)) {
          clones.push(item);
        } else {
          originals.push(item);
        }
      });

      // Kill previous animation and reset to rest state
      if ((el as any)._rotTl) {
        (el as any)._rotTl.kill();
      }
      originals.forEach((orig) => gsap.set(orig, { rotation: 0, opacity: 1 }));
      clones.forEach((clone) => gsap.set(clone, { rotation: -r, opacity: 0 }));

      // Show clones
      clones.forEach((clone) => gsap.set(clone, { opacity: 1 }));

      // Create timeline
      const tl = gsap.timeline({
        onComplete: () => {
          // Reset to rest state
          originals.forEach((orig) => gsap.set(orig, { rotation: 0, opacity: 1 }));
          clones.forEach((clone) => gsap.set(clone, { rotation: -r, opacity: 0 }));
          (el as any)._rotTl = null;
        },
      });

      // Animate originals: rotate +r and fade out
      tl.to(
        originals,
        {
          rotation: `+=${r}`,
          opacity: 0,
          duration,
          ease: 'atom',
          stagger: 0.075,
        },
        0,
      );

      // Animate clones: rotate from -r to 0 and fade in
      tl.to(
        clones,
        {
          rotation: 0,
          opacity: 1,
          duration,
          ease: 'atom',
          stagger: 0.075,
        },
        0,
      );

      (el as any)._rotTl = tl;
    };

    el.addEventListener('pointerenter', onEnter);
    listeners.push({ el, handler: onEnter });
  });

  return () => {
    listeners.forEach(({ el, handler }) => {
      el.removeEventListener('pointerenter', handler);
      if ((el as any)._rotTl) {
        (el as any)._rotTl.kill();
        (el as any)._rotTl = null;
      }
    });
  };
}
```

### Initialization Order (Critical)

```typescript
// In your main app entry (layout, _app, etc.)
import { initRotateClones, initRotateCalc, initHoverRotate } from './animations';

document.addEventListener('DOMContentLoaded', () => {
  // MUST be called in this order
  initRotateClones();  // 1. Setup clones
  initRotateCalc();    // 2. Calculate --y
  initHoverRotate();   // 3. Enable animations
});
```

### Loading Spinner (CSS Only)

```css
/* Spinner keyframe */
@keyframes button-spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* Spinner element */
.button__spinner {
  width: var(--icon-s);        /* 16px from tokens */
  height: var(--icon-s);
  border: var(--stroke-xs) solid currentColor;
  border-radius: var(--radius-pill);
  clip-path: polygon(50% 0%, 100% 0%, 100% 50%, 50% 50%); /* Quarter circle */
  animation: button-spin var(--duration-default) linear infinite;
  flex-shrink: 0;
}

@media (prefers-reduced-motion: reduce) {
  .button__spinner {
    animation-duration: 1.5s; /* Slower for reduced motion */
  }
}
```

### Shimmer Effect (GSAP Animation)

**CSS Setup:**
```css
.button__loading-content {
  display: inline-flex;
  align-items: center;
  gap: var(--s);
  white-space: nowrap;
}

.button__loading-text {
  display: inline;
}
```

**GSAP Shimmer Function:**
```typescript
import { gsap } from './config';

function prefersReducedMotion(): boolean {
  return window.matchMedia('(prefers-reduced-motion: reduce)').matches;
}

export function initShimmerText() {
  const elements = document.querySelectorAll<HTMLElement>('[data-shimmer]');
  if (!elements.length) return () => {};

  // Respect reduced motion preference
  if (prefersReducedMotion()) return () => {};

  const tweens: gsap.core.Tween[] = [];
  const styledElements: HTMLElement[] = [];

  elements.forEach((el) => {
    const duration = parseFloat(el.getAttribute('data-shimmer') || '') || 2;

    // Use semantic token colors (customizable via CSS vars)
    const baseColor = 'var(--shimmer-base, var(--fg-tertiary))';
    const shimmerColor = 'var(--shimmer-highlight, var(--fg-primary))';

    // Apply shimmer background inline
    Object.assign(el.style, {
      background: `
        linear-gradient(
          90deg,
          ${baseColor} 0%,
          ${baseColor} 35%,
          ${shimmerColor} 50%,
          ${baseColor} 65%,
          ${baseColor} 100%
        )
      `.trim(),
      backgroundSize: '300% 100%',
      backgroundClip: 'text',
      WebkitBackgroundClip: 'text',
      WebkitTextFillColor: 'transparent',
      backgroundPositionX: '100%',
    });

    styledElements.push(el);

    // Animate backgroundPositionX from 100% → 0%
    const tween = gsap.fromTo(
      el,
      { backgroundPositionX: '100%' },
      {
        backgroundPositionX: '0%',
        duration,
        ease: 'none',
        repeat: -1,
        repeatDelay: 0.3,
      },
    );

    tweens.push(tween);
  });

  return () => {
    tweens.forEach((t) => t.kill());
    styledElements.forEach((el) => {
      Object.assign(el.style, {
        background: '',
        backgroundSize: '',
        backgroundClip: '',
        WebkitBackgroundClip: '',
        WebkitTextFillColor: '',
        backgroundPositionX: '',
      });
    });
  };
}
```

**Usage:**
```typescript
// Initialize shimmer on page load
document.addEventListener('DOMContentLoaded', () => {
  initShimmerText();
});
```

## Accessibility Requirements

**ARIA attributes:**
```html
<!-- Disabled -->
<button disabled aria-disabled="true">...</button>

<!-- Loading -->
<button disabled aria-disabled="true" aria-busy="true">...</button>

<!-- Clone label -->
<span aria-hidden="true">...</span>
```

**Keyboard navigation:**
- Button must be focusable with Tab
- Activatable with Enter or Space
- Focus-visible styling required

**Motion preferences:**
- Respect `prefers-reduced-motion: reduce`
- Disable animations when OS preference is set
- Slower animation speeds for reduced motion

## Usage Examples

### Basic Button
```tsx
<Button variant="Primary" size="m">
  Click me
</Button>
```

### Button with Lucide Icon (Left)
```astro
---
import { Upload } from 'lucide-react';
import { Button } from '@atomchat.io/components-astro';
---

<Button variant="Secondary" size="l">
  <Upload slot="icon-left" size={20} client:load />
  Upload File
</Button>
```

### Button with Lucide Icon (Right)
```astro
---
import { ArrowRight } from 'lucide-react';
import { Button } from '@atomchat.io/components-astro';
---

<Button variant="Primary" size="m">
  Get Started
  <ArrowRight slot="icon-right" size={18} client:load />
</Button>
```

### Button with Icons (Both Sides)
```astro
---
import { Download, ExternalLink } from 'lucide-react';
import { Button } from '@atomchat.io/components-astro';
---

<Button variant="Tertiary" size="m">
  <Download slot="icon-left" size={18} client:load />
  Export Data
  <ExternalLink slot="icon-right" size={16} client:load />
</Button>
```

### Icon-Only Button
```astro
---
import { Settings } from 'lucide-react';
import { IconButton } from '@atomchat.io/components-astro';
---

<IconButton variant="Ghost" size="m" aria-label="Settings">
  <Settings size={20} client:load />
</IconButton>
```

### Loading Button
```tsx
<Button variant="Primary" size="m" loading>
  Submitting...
</Button>
```

### Link Button
```tsx
<Button variant="Tertiary" size="s" href="/about">
  Learn More
</Button>
```

### Disabled Button
```tsx
<Button variant="Destructive Primary" disabled>
  Delete Account
</Button>
```

## Implementation Checklist

- [ ] Install required packages (`@atomchat.io/css`, `@atomchat.io/tokens`)
- [ ] Create Button component with props API
- [ ] Implement all 6 variants with token-driven styles
- [ ] Implement all 5 sizes
- [ ] Add disabled state handling
- [ ] Add loading state with spinner
- [ ] Add icon slot support (left/right)
- [ ] Pre-render clone labels for animation
- [ ] Add proper ARIA attributes
- [ ] Implement link rendering (`href` prop → `<a>` tag)
- [ ] If animations enabled: Install GSAP
- [ ] If animations enabled: Implement hover rotation functions
- [ ] If animations enabled: Add shimmer effect
- [ ] Test keyboard navigation
- [ ] Test screen reader accessibility
- [ ] Test all states (default, hover, focus, active, disabled, loading)
- [ ] Verify all styles use tokens (no hardcoded values)
- [ ] Test responsive behavior
- [ ] Test prefers-reduced-motion

## Critical Rules

1. **NEVER hardcode values** — Every color, spacing, font size must use design tokens
2. **Pre-render clone labels** — Do not create clones with JavaScript (SSR compatibility)
3. **Maintain data-attribute contract** — Required for animation system to work
4. **Respect accessibility** — All ARIA attributes, keyboard navigation, focus states
5. **Follow separation of concerns** — Component renders markup, animations initialize globally
6. **Token-first philosophy** — If a token exists, use it. If it doesn't, request it.

## Questions After Implementation

After building the component, verify:
1. Do all variants render correctly?
2. Do all sizes scale properly?
3. Does loading state show spinner and disable interaction?
4. Do icons appear in correct positions?
5. Does href prop render as `<a>` tag?
6. If animations enabled: Does hover rotation work smoothly?
7. If animations enabled: Does shimmer effect work?
8. Does component respect prefers-reduced-motion?
9. Are all styles using design tokens?
10. Is keyboard navigation working?

---

Now, based on your answers to Questions 1 and 2, please create a complete Button component following this specification.
